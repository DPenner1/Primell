namespace dpenner1.PrimellF


// TODO not using yet, but I can see adding more functionality in the future

type PrimellSeq<'T>(sequence: seq<'T>, length: PNumber) =
  let Main = sequence
  let length = length

  
  
